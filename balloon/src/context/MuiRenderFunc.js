// mui 리스트 선택 시 업데이트용 랜더링 함수
export const updateCheck = async (setUpdateChk) => {
  setUpdateChk(true);
};
// mui 리스트 선택 시 삭제용 랜더링 함수
export const deleteCheck = async (setDeleteChk) => {
  setDeleteChk(true);
};
